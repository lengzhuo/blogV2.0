<!-- 头部区域 -->
<header>
    <% if (user) {%>
    <div class="container">
        <h3><%= user.signature%></h3>
    </div>
    <% } else {%>
    <div class="container">
        <h3>成功者永不放弃，放弃者永不成功</h3>
    </div>
    <% }%>
</header>